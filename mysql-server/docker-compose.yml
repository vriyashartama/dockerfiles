version: '3.3'
services:
  mysql:
    image: mysql:latest
    restart: unless-stopped
    container_name: mysql
    ports:
      - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - ../data/mysql:/var/lib/mysql